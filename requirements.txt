# Streamlit demo for the FTT flight delay model.
# Install with:  pip install -r requirements.txt
# (CPU-only torch — for GPU, install torch separately first from pytorch.org)

streamlit>=1.30
pytorch-tabular>=1.2
torch>=2.1
pytorch-lightning>=2.2
pandas>=2.0,<3.0
numpy>=1.26
joblib>=1.3
pyarrow>=14
scikit-learn>=1.6
